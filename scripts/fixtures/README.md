# Fixtures PEM somente para testes locais da Fase A Inter.
# NÃO são credenciais reais do Banco Inter.
# Regenerar: openssl req -x509 -newkey rsa:2048 -keyout inter-test-key.pem -out inter-test-cert.pem -days 2 -nodes -subj "/CN=svlotes-inter-fase-a-test"
