{
  "name": "sv-lotes-inter-webhook-receiver",
  "version": "1.0.0",
  "private": true,
  "description": "Receptor HTTPS mTLS para callbacks Cobrança V3 do Banco Inter (SV LOTES Fase C)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
